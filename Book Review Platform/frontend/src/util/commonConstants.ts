export const PASSWORD_REGEX = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/;

export const SUPPORTED_IMAGE_FORMATS = ['image/jpeg', 'image/png', 'image/gif', 'image/svg+xml', 'image/webp'];
export const GENRE = ['Fiction', 'Non-Fiction', 'Drama', 'Poetry'];

export const MAX_IMAGE_SIZE = 15 * 1024 * 1024;
